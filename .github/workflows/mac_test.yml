name: Interactive Mac Terminal (Upterm)

on:
  workflow_dispatch:

jobs:
  upterm-mac:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Start Upterm Session
        uses: lhotari/action-upterm@v1
